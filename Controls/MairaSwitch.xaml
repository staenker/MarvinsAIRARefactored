<UserControl x:Class="MarvinsAIRARefactored.Controls.MairaSwitch"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:MarvinsAIRARefactored.Controls"
             mc:Ignorable="d">

    <Grid x:Name="LayoutRoot">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Button Grid.Row="1"
                Grid.Column="0"
                x:Name="Button"
                Focusable="False"
                IsTabStop="False"
                Background="Transparent"
                BorderThickness="0"
                HorizontalAlignment="Left"
                Click="Button_Click">

            <Button.Style>

                <Style TargetType="Button">

                    <Setter Property="OverridesDefaultStyle"
                            Value="True" />
                    <Setter Property="Cursor"
                            Value="Hand" />

                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>

                </Style>

            </Button.Style>

            <Border Width="24"
                    Height="24"
                    BorderThickness="2"
                    CornerRadius="5">

                <Border.Style>

                    <Style TargetType="Border">

                        <Setter Property="BorderBrush"
                                Value="#666" />
                        <Setter Property="Background"
                                Value="Transparent" />

                        <Style.Triggers>

                            <DataTrigger Binding="{Binding IsOn, ElementName=Root}"
                                         Value="True">
                                <Setter Property="BorderBrush"
                                        Value="#eee" />
                                <Setter Property="Background"
                                        Value="#ff5b2e" />
                            </DataTrigger>

                        </Style.Triggers>

                    </Style>

                </Border.Style>

                <TextBlock FontSize="20"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center">

                    <TextBlock.Style>

                        <Style TargetType="TextBlock"
                               BasedOn="{StaticResource MairaTextBlockStyle}">

                            <Setter Property="Text"
                                    Value="" />

                            <Style.Triggers>

                                <DataTrigger Binding="{Binding IsOn, ElementName=Root}"
                                             Value="True">
                                    <Setter Property="Text"
                                            Value="✓" />
                                </DataTrigger>

                            </Style.Triggers>

                        </Style>

                    </TextBlock.Style>

                </TextBlock>

            </Border>

        </Button>

        <TextBlock x:Name="TextBlock"
                   Text="{Binding Label, ElementName=Root, FallbackValue='Just a test to see what this looks like'}"
                   TextTrimming="CharacterEllipsis"
                   VerticalAlignment="Center"
                   Foreground="#fff"
                   Cursor="Hand"
                   TextWrapping="Wrap"
                   MouseDown="Button_Click"
                   PreviewMouseRightButtonDown="TextBlock_PreviewMouseRightButtonDown">

            <TextBlock.Style>

                <Style TargetType="TextBlock" BasedOn="{StaticResource MairaTextBlockStyle}">

                    <Setter Property="Grid.Row"
                            Value="1" />
                    <Setter Property="Grid.Column"
                            Value="1" />
                    <Setter Property="Margin"
                            Value="10,0,0,0" />

                    <Style.Triggers>

                        <DataTrigger Binding="{Binding LabelPosition, ElementName=Root}"
                                     Value="Top">
                            <Setter Property="Grid.Row"
                                    Value="0" />
                            <Setter Property="Grid.Column"
                                    Value="0" />
                            <Setter Property="Margin"
                                    Value="0,0,0,5" />
                        </DataTrigger>

                    </Style.Triggers>
                    
                </Style>
                
            </TextBlock.Style>
            
        </TextBlock>

    </Grid>

</UserControl>

<Window x:Class="MarvinsAIRARefactored.Windows.SpeechToTextWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarvinsAIRARefactored.Windows"
        xmlns:datacontext="clr-namespace:MarvinsAIRARefactored.DataContext"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Loaded="Window_Loaded"
        LocationChanged="Window_LocationChanged"
        Cursor="SizeAll">

    <Window.DataContext>
        <Binding Source="{x:Static datacontext:DataContext.Instance}"/>
    </Window.DataContext>

    <Grid>

        <Grid.LayoutTransform>
            <ScaleTransform x:Name="RootScaleTransform" ScaleX="{Binding Settings.SpeechToTextOverlayWindowScale}" ScaleY="{Binding Settings.SpeechToTextOverlayWindowScale}"/>
        </Grid.LayoutTransform>

        <Border Width="1024" BorderThickness="2" BorderBrush="White" Background="Black" CornerRadius="10" Padding="10" HorizontalAlignment="Center" VerticalAlignment="Top">
            <TextBlock x:Name="TextBlock" d:Text="This is some text inside the border. The border will always be 1024 pixels wide, but its height grows automatically to wrap the text content." TextWrapping="Wrap" FontSize="24" Foreground="White"/>
        </Border>

    </Grid>

</Window>

<UserControl x:Class="MarvinsAIRARefactored.Pages.OverlaysPage"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MarvinsAIRARefactored.Pages"
             xmlns:controls="clr-namespace:MarvinsAIRARefactored.Controls"
             xmlns:datacontext="clr-namespace:MarvinsAIRARefactored.DataContext"
             mc:Ignorable="d"
             d:DesignWidth="800">

    <UserControl.DataContext>
        <Binding Source="{x:Static datacontext:DataContext.Instance}" />
    </UserControl.DataContext>

    <StackPanel>

        <controls:MairaGroupBox Label="{Binding Localization[GapMonitor_UC]}">

            <Grid>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="20" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0"
                            Grid.Column="0"
                            Grid.ColumnSpan="3">

                    <controls:MairaSwitch IsOn="{Binding Settings.OverlaysShowGapMonitorWindow, Mode=TwoWay}"
                                          Label="{Binding Localization[ShowGapMonitorWindow]}" />

                    <controls:MairaSwitch Margin="0,10,0,0"
                                          IsOn="{Binding Settings.OverlaysMakeGapMonitorDraggable, Mode=TwoWay}"
                                          Label="{Binding Localization[MakeGapMonitorWindowDraggable]}" />

                    <controls:MairaSwitch Margin="0,10,0,0"
                                          IsOn="{Binding Settings.OverlaysShowGapMonitorTitle, Mode=TwoWay}"
                                          Label="{Binding Localization[ShowGapMonitorWindowTitle]}" />

                </StackPanel>

                <controls:MairaKnob Grid.Row="0"
                                    Grid.Column="4"
                                    Label="{Binding Localization[OverlayWindowScale]}"
                                    Value="{Binding Settings.OverlaysGapMonitorWindowScale, Mode=TwoWay}"
                                    ValueString="{Binding Settings.OverlaysGapMonitorWindowScaleString}"
                                    StepSize="0.05"
                                    DefaultValue="1" />

                <controls:MairaButton x:Name="ResetGapMonitorWindow_MairaButton"
                                      Grid.Row="2"
                                      Grid.Column="0"
                                      Grid.ColumnSpan="5"
                                      Icon="/MarvinsAIRARefactored;component/Artwork/Buttons/reset.png"
                                      Label="{Binding Localization[ResetGapMonitorWindow]}"
                                      Click="ResetGapMonitorWindow_MairaButton_Click" />

            </Grid>

        </controls:MairaGroupBox>

    </StackPanel>

</UserControl>
